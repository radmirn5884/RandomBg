<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RandomBg" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Полноэкранный режим -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>

        <!-- Отключаем ActionBar (если он мешает) -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Важно: скрываем и навигацию, и статус-бар -->
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>

    <style name="Theme.RandomBg" parent="Base.Theme.RandomBg" />

    <style name="Theme.FullScreen" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item> <!-- Для вырезов (камеры) -->
    </style>
</resources>